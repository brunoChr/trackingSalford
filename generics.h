#ifndef GENERICS_H
#define GENERICS_H

#define	TRUE			1
#define	FALSE			0

#define MIN(X, Y)		((X < Y) ? X : Y)
#define MAX(X, Y)		((X > Y) ? X : Y)
#define SIZE(x)			(sizeof(x) / sizeof(x[0]))

#endif
